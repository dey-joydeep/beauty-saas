{
  "name": "web-admin-e2e",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/e2e/web/admin/src",
  "targets": {
    "e2e": {
      "executor": "@nx/run-commands:run-commands",
      "options": {
        "commands": [
          {
            "command": "echo Web Admin E2E: scaffolding in place; tests TBD"
          }
        ],
        "parallel": false
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/e2e/web/admin/tsconfig.json --noEmit"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["apps/e2e/web/admin/**/*.ts"]
      }
    }
  },
  "implicitDependencies": ["web-admin"],
  "tags": ["type:e2e", "platform:web"]
}
