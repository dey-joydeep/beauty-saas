{
  "name": "@beauty-saas/partner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "ng serve",
    "build": "ng build",
    "test": "nx jest:test --config=jest.config.ts --passWithNoTests",
    "lint": "nx eslint:lint",
    "build:ssr": "ng build && ng run partner:server",
    "serve:ssr": "node dist/apps/web/partner/server/server.mjs"
  }
}
