{
  "name": "@beauty-saas/web-customer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "ng serve",
    "build": "ng build",
    "test": "nx jest:test --config=jest.config.ts --passWithNoTests",
    "lint": "nx eslint:lint",
    "build:ssr": "ng build && ng run web-customer:server",
    "serve:ssr": "node dist/apps/web/customer/server/server.mjs"
  }
}
