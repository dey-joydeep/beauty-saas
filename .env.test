# Test Database Configuration
DATABASE_URL="postgresql://postgres:root@localhost:5432/bsaas_test?schema=public"

# Test Application Settings
NODE_ENV=test
PORT=3001
JWT_SECRET=test_secret_key

# Test Email Configuration (using ethereal.email for testing)
EMAIL_USER=test@example.com
EMAIL_PASS=test_password
EMAIL_FROM="Test Environment <test@example.com>"

# Test Application URLs (comma-separated for multiple frontends in test)
FRONTEND_URLS="http://localhost:9876,http://localhost:9877,http://localhost:9878"
API_URL=http://localhost:3001

# Test File Storage Paths
UPLOAD_DIR=test-uploads
BASE_IMAGE_PATH="${UPLOAD_DIR}/salon/${salonId}"
PORTFOLIO_IMAGE_PATH="portfolio/${portfolioId}/images"
STAFF_IMAGE_PATH="staffs/${staffId}/images"

# Test CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS="http://localhost:9876,http://localhost:9877,http://localhost:9878"

# Test Logging
LOG_LEVEL=warn
LOG_FILE=logs/test.log

# Test Rate Limiting (more permissive for testing)
RATE_LIMIT_WINDOW_MS=60000  # 1 minute
RATE_LIMIT_MAX=1000  # Higher limit for testing

# Test-specific settings
TEST_TIMEOUT=5000  # ms
TEST_DATABASE_RESET=true